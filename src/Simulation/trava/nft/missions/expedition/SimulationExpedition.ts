import { ApplicationState } from "../../../../../State";

export async function simulateExpedition(appState1: ApplicationState, _ids: Array<number>, _level: number) {
    let appState = appState1;

    try {

    } catch(err) {
        console.log(err);
    }
    return appState;
}
